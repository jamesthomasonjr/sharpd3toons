<html>
  <head>
    <title>{{ account }}</title>
  </head>
  <body>
    <h1>{{ account }}</h1>
    <br>
    <p>
      {% for hero in profile.heroes %}
        {{ hero.name }} [{{ hero.level }}]<br>
      {% endfor %}
    </p>
  </body>
</html>
